<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pago Rechazado | Akalia</title>
    <%- include('../partials/head.ejs'); %>
    <link rel="stylesheet" href="/styles/funciones-usuarios.css">
    <style>
        .failure-container {
            min-height: 70vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .failure-card {
            background: white;
            border-radius: 16px;
            padding: 3rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            max-width: 600px;
            text-align: center;
        }

        .failure-icon {
            width: 80px;
            height: 80px;
            background: #dc3545;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            animation: shake 0.5s ease-out;
        }

        @keyframes shake {
            0%, 100% {
                transform: translateX(0);
            }
            25% {
                transform: translateX(-10px);
            }
            75% {
                transform: translateX(10px);
            }
        }

        .failure-icon svg {
            width: 40px;
            height: 40px;
            color: white;
        }

        .error-details {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            border-radius: 8px;
            padding: 1.5rem;
            margin: 1.5rem 0;
            text-align: left;
        }

        .reasons-list {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 1.5rem;
            margin: 1.5rem 0;
            text-align: left;
        }

        .reasons-list ul {
            margin: 0;
            padding-left: 1.5rem;
        }

        .reasons-list li {
            margin-bottom: 0.5rem;
            color: #6c757d;
        }
    </style>
</head>

<body class="bg-blanco-light">
    <%- include('../partials/navbar.ejs'); %>

    <main class="failure-container">
        <div class="container">
            <div class="failure-card">
                <!-- Failure Icon -->
                <div class="failure-icon">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 16 16">
                        <path d="M4.646 4.646a.5.5 0 0 1 .708 0L8 7.293l2.646-2.647a.5.5 0 0 1 .708.708L8.707 8l2.647 2.646a.5.5 0 0 1-.708.708L8 8.707l-2.646 2.647a.5.5 0 0 1-.708-.708L7.293 8 4.646 5.354a.5.5 0 0 1 0-.708z"/>
                    </svg>
                </div>

                <!-- Failure Message -->
                <h1 class="akalia-title akalia-title--medium mb-3">Pago Rechazado</h1>
                <p class="text-muted mb-4">Lo sentimos, no pudimos procesar tu pago. Por favor, verifica tus datos e intenta nuevamente.</p>

                <!-- Error Details -->
                <% if (typeof error !== 'undefined' && error) { %>
                <div class="error-details">
                    <h6 class="mb-2">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-exclamation-triangle-fill me-2" viewBox="0 0 16 16">
                            <path d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
                        </svg>
                        <strong>Detalles del Error:</strong>
                    </h6>
                    <p class="mb-0 small"><%= error.message || error %></p>
                </div>
                <% } %>

                <!-- Possible Reasons -->
                <div class="reasons-list">
                    <h5 class="mb-3">Posibles causas:</h5>
                    <ul>
                        <li>Fondos insuficientes en tu cuenta</li>
                        <li>Datos de tarjeta incorrectos</li>
                        <li>La tarjeta ha expirado</li>
                        <li>El banco rechazó la transacción</li>
                        <li>Límite de compra excedido</li>
                    </ul>
                </div>

                <!-- Action Buttons -->
                <div class="d-flex flex-column flex-sm-row gap-3 justify-content-center mt-4">
                    <button onclick="history.back()" class="btn btn-terracota">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-counterclockwise me-2" viewBox="0 0 16 16">
                            <path fill-rule="evenodd" d="M8 3a5 5 0 1 1-4.546 2.914.5.5 0 0 0-.908-.417A6 6 0 1 0 8 2v1z"/>
                            <path d="M8 4.466V.534a.25.25 0 0 0-.41-.192L5.23 2.308a.25.25 0 0 0 0 .384l2.36 1.966A.25.25 0 0 0 8 4.466z"/>
                        </svg>
                        Intentar de nuevo
                    </button>
                    <a href="/" class="btn btn-outline-secondary">
                        Volver al inicio
                    </a>
                </div>

                <!-- Help Section -->
                <div class="mt-4 p-3 bg-light rounded">
                    <small class="text-muted">
                        <strong>¿Necesitas ayuda?</strong> Contáctanos a través de nuestra 
                        <a href="/contacto" class="text-decoration-none">página de contacto</a> o envíanos un correo.
                    </small>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer.ejs'); %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
